# Ranges are used rather than exact pins so that a single file resolves across the
# supported Python versions (3.11-3.14), as numpy/pandas wheel availability varies.

faicons>=0.2.2,<0.3
flatten-json>=0.1.14,<0.2
numpy>=2.0,<3.0
pandas>=3.0,<4.0
requests>=2.31.0,<3.0
rich-click>=1.5.2,<2.0
shiny>=0.9.0,<0.10
shinyswatch>=0.4.2,<0.5
Jinja2>=3.1.3,<4.0
openpyxl>=3.0.10,<4.0